1、此类方法习惯以Template为后缀，如JdbcTemplate,HibernateTemplate
2、在父类中定义处理流程的框架，在子类中实现集体处理
3、模板方法基于继承，父类中定义几个大步骤的执行顺序逻辑，方法中多余一个的大步骤的，子类中是这些每个大步骤的具体实现
    方法以doXXX开头，如果只有一个大步骤，则会演化为策略模式
   策略模式基于委托，在一个接口中定义一个方法而没有逻辑，子类做具体实现